{
  "name": "clonecraft-codegen-agent",
  "private": true,
  "version": "0.1.0",
  "description": "eve agent that owns CloneCraft's generate→build→fix→preview loop. Runs as its own service; the Hono pipeline calls it over HTTP.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "eve dev",
    "build": "eve build",
    "start": "eve start",
    "info": "eve info",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ai": "7.0.0-beta.178",
    "e2b": "^2.29.1",
    "eve": "^0.11.10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
